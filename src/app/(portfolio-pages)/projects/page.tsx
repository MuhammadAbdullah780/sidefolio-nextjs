import React from "react";

// Components
import ProjectListingContainer from "@/containers/Projects";

type Props = {};

const Page = (props: Props) => {
  return <ProjectListingContainer />;
};

export default Page;
